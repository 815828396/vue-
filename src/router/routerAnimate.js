
export default (to, from) => {
  const __path = to.path.slice(1)

  if (  __path.toLowerCase() === 'insbaoming' ) {
    return 'pulse'
  }
  // if (  __path.toLowerCase() === 'insbaoming' ) {
  //   return ''
  // }
}
